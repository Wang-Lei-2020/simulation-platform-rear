<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bjtu.simulation_platform_rear.dao.IUserDao">
    <select id="findByUserName" parameterType="String" resultType="User">
        select * from user where userName = #{userName}
    </select>
    <insert id="register" parameterType="User" useGeneratedKeys="true" keyProperty="id">
        insert into user(userName,realName,password,sex,email,phone,description,logoImage,role) values (#{userName},#{realName},#{password},#{sex},#{email},#{phone},#{description},#{logoImage},#{role})
    </insert>
    <select id="countByRealName" parameterType="String" resultType="Integer">
        select count(*) from user where realName like concat('%',#{realName},'%')
    </select>
    <select id="searchByRealName" parameterType="String" resultType="User">
        select * from user where realName like concat('%',#{realName},'%')
    </select>
    <select id="getUserList" parameterType="int" resultType="User">
        select * from user limit #{from},#{num}
    </select>
    <select id="getUserCount" resultType="Integer">
        select count(*) from user
    </select>
    <select id="getPasswordById" parameterType="int" resultType="String">
        select password from user where id = #{id}
    </select>
    <update id="update" parameterType="User">
        update user set userName=#{userName},realName=#{realName},password=#{password},sex=#{sex},phone=#{phone},email=#{email},description=#{description} where id = #{id}
    </update>
</mapper>